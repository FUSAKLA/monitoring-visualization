

function neo4jToGraph(neo_graph) {
    var neo_nodes = neo_graph.nodes,
        neo_edges = neo_graph.links,
        graph = {nodes:[], links:[]};

    neo_nodes.forEach(function(node){

    });

}